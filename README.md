
  # BridgeTheBite Web Platform UI

  This is a code bundle for BridgeTheBite Web Platform UI. The original project is available at https://www.figma.com/design/zstBLbPbVAyP2eYX29XTit/BridgeTheBite-Web-Platform-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  